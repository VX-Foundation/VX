{
  "name": "vx-benchmark-sveltekit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "node driver.mjs --prepare",
    "build": "node driver.mjs --build",
    "start": "node driver.mjs --start"
  },
  "dependencies": {
    "@sveltejs/kit": "$stable",
    "svelte": "$stable"
  },
  "devDependencies": {
    "vite": "$stable",
    "@sveltejs/adapter-node": "$stable"
  }
}
