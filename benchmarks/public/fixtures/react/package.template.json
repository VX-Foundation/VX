{
  "name": "vx-benchmark-react",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "node driver.mjs --prepare",
    "build": "node driver.mjs --build",
    "start": "node driver.mjs --start"
  },
  "dependencies": {
    "react": "$stable",
    "react-dom": "$stable"
  },
  "devDependencies": {
    "vite": "$stable",
    "@vitejs/plugin-react": "$stable"
  }
}
