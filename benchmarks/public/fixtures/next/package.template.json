{
  "name": "vx-benchmark-next",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "node driver.mjs --prepare",
    "build": "node driver.mjs --build",
    "start": "node driver.mjs --start"
  },
  "dependencies": {
    "next": "$stable",
    "react": "$stable",
    "react-dom": "$stable"
  }
}
