{
  "name": "vx-benchmark-vx",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "node driver.mjs --prepare",
    "build": "node driver.mjs --build",
    "start": "node driver.mjs --start"
  },
  "dependencies": {
    "@vx-foundation/runtime": "$stable",
    "@vx-foundation/router": "$stable",
    "@vx-foundation/server": "$stable"
  },
  "devDependencies": {
    "@vx-foundation/cli": "$stable"
  }
}
