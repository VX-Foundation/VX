{
  "name": "vx-benchmark-solid",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "node driver.mjs --prepare",
    "build": "node driver.mjs --build",
    "start": "node driver.mjs --start"
  },
  "dependencies": {
    "solid-js": "$stable"
  },
  "devDependencies": {
    "vite": "$stable",
    "vite-plugin-solid": "$stable"
  }
}
