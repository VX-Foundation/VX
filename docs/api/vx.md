# @vx-foundation/vx

Compiler-first full-stack web framework with direct DOM rendering, deterministic SSR, typed data primitives, and production deployment adapters.

Current package line: `0.1.3`.

## Public entries

- `.` -> `./index.d.ts`

## Exported symbols

No statically discoverable named TypeScript exports.

## Stability

Only entries listed above are public. Imports from `src`, `dist`, or undeclared files are unsupported and rejected by the official-application gate.
