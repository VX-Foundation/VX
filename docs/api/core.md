# @vx-foundation/core

VX Core Orchestration

Current package line: `0.1.1`.

## Public entries

- `.` -> `./dist/index.d.ts`
- `./package` -> `./dist/package.d.ts`

## Exported symbols

- `BuildOverrides` - type in `index.ts`
- `Context` - class in `integration.ts`
- `defineConfig` - function in `config.ts`
- `packageLibrary` - function in `package.ts`
- `PackageLibraryOptions` - type in `package.ts`
- `PreviewHandle` - interface in `preview.ts`
- `PreviewOptions` - interface in `preview.ts`

## Stability

Only entries listed above are public. Imports from `src`, `dist`, or undeclared files are unsupported and rejected by the official-application gate.
