# @vx-foundation/create-vx

Scaffolding tool for VX framework

Current package line: `0.1.2`.

## Public entries

- Package root only.

## Exported symbols

- `CreateVXRuntime` - interface in `index.ts`
- `normalizeCreateArguments` - function in `index.ts`
- `runCreateVX` - function in `index.ts`

## Stability

Only entries listed above are public. Imports from `src`, `dist`, or undeclared files are unsupported and rejected by the official-application gate.
