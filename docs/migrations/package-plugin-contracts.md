# Package and Plugin Contracts

Move undeclared subpath imports to published exports. Add public contract metadata, integrity, provenance, deprecation, and migration records.

Plugins must move filesystem and environment access to mediated capabilities, declare permissions, use deterministic cache keys, and adopt detached code-integrity signatures.
