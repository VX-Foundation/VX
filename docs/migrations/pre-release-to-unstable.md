# Pre-release to Unstable

Replace obsolete regions such as `#state`, `#logic`, `#data`, and `#style` with `#script`, `#view`, declarations, and Visual IR roles.

Replace symbolic plugins with supported public plugins or native framework features. Regenerate `vx.lock`, run `vx migrate`, review every diagnostic, and accept generated changes only after tests pass.
