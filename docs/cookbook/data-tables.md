# Data Tables

Keep sort, filters, cursor, and page size in explicit state. Build a deterministic query key. Use keyed rows, accessible headers, server-side authorization, and cancellation when parameters change.
