# Authentication and Permissions

Resolve an opaque signed session on the server, establish a principal after credential verification, regenerate the session, and enforce permissions on every protected operation with `authorizeServerRequest` or authorization middleware.
