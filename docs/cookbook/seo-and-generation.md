# SEO and Generation

Use deterministic metadata, canonical URLs, semantic headings, structured data, sitemap output, and static or incremental generation only for routes that do not depend on private request state.
