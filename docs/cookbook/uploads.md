# Uploads

Use multipart server forms or endpoints with explicit byte limits. Validate content type and signature, isolate storage keys, process media outside the request when appropriate, and never trust the original filename.
