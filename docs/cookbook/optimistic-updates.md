# Optimistic Updates

Apply optimistic changes through an action transaction. Record rollback before the network call, use an idempotency key, reconcile the authoritative response, and invalidate tags after success.
