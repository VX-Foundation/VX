# Forms

Use typed schemas, client controllers, and server form contracts. The server decodes bounded input, blocks prototype-pollution keys, validates the schema, authorizes the operation, verifies CSRF, and returns structured field or form errors.

Uploads are always server-governed. Browser accept filters are UX hints, not security validation.
