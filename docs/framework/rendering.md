# Rendering and Hydration

VX emits direct DOM updates and deterministic SSR markup. Hydration can be full, lazy, idle, visible, or interaction-triggered. Islands and resumable boundaries are explicit route or component policies.

Hydration recovery preserves focus, form values, checked state, and selection when possible. External DOM changes from extensions or third-party scripts are classified in diagnostics.
