# Configuration

Use `defineConfig` from `@vx-foundation/core`. Configure adapter, targets, source maps, deterministic and reproducible builds, assets, chunk policy, plugin contracts, and library entrypoints.

Configuration is validated before destructive build work. `outDir` cannot escape the project or target its root.
