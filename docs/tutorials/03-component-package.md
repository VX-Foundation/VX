# Publish a Component Package

Run `vx new my-ui --template library`. Add typed props, outputs, named content, and public parts. Keep helper modules private.

Run `vx check`, `vx test`, `vx build`, `vx package`, the API compatibility check, and provenance generation. Publish first to a prerelease channel and verify installation in a clean project.
