# Create a Routed Application

Run `vx new my-app --template starter`, install locked dependencies, then run `vx doctor`, `vx check src`, and `vx dev`.

Add `src/pages/reports/page.vx` for `/reports`. Use `layout.vx` for shared navigation. Keep route rendering policy in `route.json`, then verify with `vx inspect` and `vx build`.
