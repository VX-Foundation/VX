# Package Authoring

Place public visual components in conventional component directories and headless modules in public module directories. Export only stable contracts. Keep private helpers private.

Validate SemVer changes, generate declarations, package deterministically, record integrity and provenance, sign releases where required, and include deprecation plus migration metadata before removing APIs.
