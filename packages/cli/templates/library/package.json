{
  "name": "vx-library-template",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "check": "vx check src",
    "lint": "vx lint src",
    "format": "vx format src",
    "format:check": "vx format src --check",
    "doctor": "vx doctor",
    "test": "vx test:component src/components/Card.vx",
    "package": "vx package",
    "verify": "vx doctor && vx check src && vx lint src && vx test:component src/components/Card.vx && vx build && vx package",
    "build": "vx build"
  },
  "peerDependencies": {
    "@vx-foundation/runtime": "workspace:*"
  },
  "devDependencies": {
    "@vx-foundation/cli": "workspace:*",
    "@vx-foundation/core": "workspace:*",
    "@vx-foundation/runtime": "workspace:*",
    "@vx-foundation/tooling": "workspace:*",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22.11.0 <23 || >=24.11.0 <25",
    "pnpm": ">=11.19.0 <12"
  },
  "packageManager": "pnpm@11.19.0"
}
