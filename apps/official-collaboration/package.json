{
  "name": "@vx/official-collaboration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vx dev",
    "build": "vx build",
    "preview": "vx preview",
    "check": "vx check src",
    "lint": "vx lint src",
    "format": "vx format src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "verify": "pnpm check && pnpm lint && pnpm test && pnpm build"
  },
  "dependencies": {
    "@vx/core": "workspace:*",
    "@vx/data": "workspace:*",
    "@vx/forms": "workspace:*",
    "@vx/router": "workspace:*",
    "@vx/runtime": "workspace:*",
    "@vx/server": "workspace:*"
  },
  "devDependencies": {
    "@vx/cli": "workspace:*",
    "@vx/testing": "workspace:*",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=22.11.0 <23 || >=24.11.0 <25"
  }
}
