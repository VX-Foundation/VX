{
  "name": "@vx-foundation/official-collaboration",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vx dev",
    "build": "vx build",
    "preview": "vx preview",
    "check": "vx check src",
    "lint": "vx lint src",
    "format": "vx format src",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "verify": "pnpm check && pnpm lint && pnpm test && pnpm build"
  },
  "dependencies": {
    "@vx-foundation/core": "workspace:*",
    "@vx-foundation/data": "workspace:*",
    "@vx-foundation/forms": "workspace:*",
    "@vx-foundation/router": "workspace:*",
    "@vx-foundation/runtime": "workspace:*",
    "@vx-foundation/server": "workspace:*"
  },
  "devDependencies": {
    "@vx-foundation/cli": "workspace:*",
    "@vx-foundation/testing": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22.11.0 <23 || >=24.11.0 <25"
  }
}
